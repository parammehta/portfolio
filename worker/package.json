{
  "name": "portfolio-analytics-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that records portfolio custom events to Analytics Engine",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "verify": "node scripts/verify-queries.mjs",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
